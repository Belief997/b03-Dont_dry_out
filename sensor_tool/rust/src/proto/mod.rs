//! 与固件对接的两套协议 —— **互不相干**, 各有独立版本号。
//!
//! | 模块 | 协议 | 载体 | 方向 | 文档 |
//! |------|------|------|------|------|
//! | [`adv`] | 广播协议 v0x02 | 不可连接广播的厂商自定义段 | 单向 | `dev/note/05-*.md` |
//! | [`cmd`] | 命令协议 v1 | 连接后的 NUS 特性 | 双向 | `dev/note/06-*.md` |
//!
//! ⚠ 别把两者的概念互相套用。最容易混的一处: 广播协议 v0x03(已设计未接线)里
//!   也有"记录"的概念(8 条 × 2 字节 = 类型 + 克数), 那是为了塞进 31 字节而极度
//!   压缩的摘要; 而命令协议的 `REC_READ` 下载的是完整原始记录(20 字节/条,
//!   含三通道原始计数)。同名不同物。
//!
//! ⚠ 本目录是**纯逻辑, 不碰 IO** —— 全部可用 `cargo test` 覆盖, 不需要真设备。
//!   BLE 相关的一切在 `crate::ble`。

pub mod adv;
pub mod cmd;
